{
  "$comment": "MCP client configuration — populated in step 6. Each server entry is a Model Context Protocol endpoint (Senpi, Paybox, FTP Tech, etc.). Left empty in v1 so nothing tries to connect at startup.",
  "servers": []
}
